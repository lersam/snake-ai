"""Removed: use `train_sb3.py` instead.

This module was intentionally removed and should no longer be imported. Importing it will
raise an ImportError directing users to the consolidated runner in `train_sb3.py`.
"""
raise ImportError("ai.actr.run_actr_agent has been removed; use train_sb3.py in the project root instead")
